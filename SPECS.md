# Specifications & Acceptance Criteria

Every feature has an identifier, a statement of intent, and acceptance criteria written as
assertions rather than adjectives. A criterion is met only when a named test asserts it — the
**Test** column gives the function name, and every name in this document exists in
`backend/tests/`.

**Status legend**

| Mark | Meaning |
| :--- | :--- |
| ✅ | Implemented and asserted by the named test |
| 🔍 | Verified manually; no automated assertion exists |
| ⛔ | Deliberately out of scope, with the reason stated |

**Suite at a glance** — 124 tests: 114 hermetic (the default run), 7 requiring a live gateway
(`-m gateway`), 3 requiring LLM credentials (`-m llm`).

Companion documents: [CLAUDE.md](CLAUDE.md) · [README.md](README.md) ·
[MIGRATION_PLAN.md](MIGRATION_PLAN.md) · [DESIGN.md](DESIGN.md)

---

## 1. Data layer

### DATA-1 — Deterministic synthetic dataset

Companies and filings are generated by `Faker` from a fixed seed and must be reproducible on any
machine. Every golden expectation in the suite rests on this.

| # | Acceptance criterion | Test | |
| :-- | :--- | :--- | :-- |
| 1.1 | `SEED = 2025` is unchanged | `test_seed_constants` | ✅ |
| 1.2 | Seeding twice produces identical rows | `test_seed_determinism` | ✅ |
| 1.3 | `market_cap == last_price × shares_outstanding` for every company | `test_market_cap_identity` | ✅ |
| 1.4 | `shares_outstanding` is banded per company, not one flat range | `test_shares_outstanding_is_banded_per_company` | ✅ |
| 1.5 | Every company has filings | `test_filings_exist_for_every_company` | ✅ |
| 1.6 | Two independent installs produce the same data | — | 🔍 verified during the from-scratch clone test: digest `18da78b3c2e4fbcf`, 41 companies |

### DATA-2 — Pure calculation core

All financial mathematics lives in `core/calculations.py` as pure functions with no I/O, so it can
be tested without a database.

| # | Acceptance criterion | Test | |
| :-- | :--- | :--- | :-- |
| 2.1 | Ratios are computed from a company and a filing | `test_financial_ratios_basic` | ✅ |
| 2.2 | Division by zero yields `None`, never an exception | `test_financial_ratios_zero_revenue_is_safe` | ✅ |
| 2.3 | QoQ and YoY growth are derived from quarterly filings | `test_revenue_growth_qoq_yoy` | ✅ |
| 2.4 | Growth requires at least two quarters | `test_revenue_growth_needs_two_quarters` | ✅ |
| 2.5 | Sector ranking orders correctly and validates its metric | `test_sector_ranking_orders_and_validates` | ✅ |
| 2.6 | Comparison highlights the leader per dimension | `test_compare_companies_highlights` | ✅ |
| 2.7 | The data layer signals absence with one exception type | `test_data_layer_raises_data_error` | ✅ |

---

## 2. Module registry

### REG-1 — Zero-touch module addition

Dropping a package under `backend/modules/` wires it into REST, MCP and the seeder with no other
file edited. This is the project's central extensibility claim, so it is asserted by creating a
module at test time rather than by inspection.

| # | Acceptance criterion | Test | |
| :-- | :--- | :--- | :-- |
| 1.1 | `discover_modules()` returns every package exposing a `ModuleSpec` | `test_all_modules_are_discovered` | ✅ |
| 1.2 | Specs sort by `(priority, name)` — deterministic even when priorities collide | `test_discovery_order_is_deterministic` | ✅ |
| 1.3 | A package written during the test appears in the tool surface with no shared file edited | `test_dropping_in_a_module_wires_it_up` | ✅ |
| 1.4 | Every field except `name` is optional | `test_module_spec_defaults_are_all_optional` | ✅ |

### REG-2 — Capability hooks

`ModuleSpec` carries optional `register_tools`, `register_resources` and `register_prompts` hooks.
Infrastructure gets no exemption: `observability` is an ordinary router-only module.

| # | Acceptance criterion | Test | |
| :-- | :--- | :--- | :-- |
| 2.1 | All three hook types are wired by the same mechanism | `test_register_all_counts_hooks` | ✅ |
| 2.2 | A capability-only module needs no router; a router-only module needs no tools | `test_module_capability_flags` | ✅ |

---

## 3. Protocol conformance — MCP 2026-07-28

### MCP-1 — Streamable HTTP transport

| # | Acceptance criterion | Test | |
| :-- | :--- | :--- | :-- |
| 1.1 | REST, MCP and the audit API are served by one process | `test_rest_and_mcp_share_one_process` | ✅ |
| 1.2 | `GET` and `DELETE` on `/mcp` return `405` | `test_get_and_delete_are_method_not_allowed` | ✅ |
| 1.3 | `Mcp-Session-Id` and `Last-Event-ID` are ignored and never echoed | `test_session_and_resumption_headers_are_ignored` | ✅ |
| 1.4 | An invalid `Origin` is rejected with `403` (DNS-rebinding protection) | `test_invalid_origin_is_forbidden` | ✅ |
| 1.5 | A configured browser origin is accepted | `test_allowed_origin_passes` | ✅ |
| 1.6 | The shipped client works against the real endpoint, not only in memory | `test_sdk_client_over_http` | ✅ |

### MCP-2 — Discovery and identity

| # | Acceptance criterion | Test | |
| :-- | :--- | :--- | :-- |
| 2.1 | `server/discover` answers with no prior request | `test_server_discover_over_http` | ✅ |
| 2.2 | The negotiated revision is `2026-07-28` | `test_protocol_version_is_2026_07_28` | ✅ |
| 2.3 | Capabilities cover tools, resources and prompts | `test_capabilities_cover_all_three_surfaces` | ✅ |
| 2.4 | `serverInfo` reports `mcp-market-mcp-server`; `instructions` is carried unchanged | `test_server_identity_and_instructions` | ✅ |
| 2.5 | Client identity travels on every request, not at a handshake | `test_client_info_is_carried_per_request` | ✅ |

### MCP-3 — Request metadata and errors

| # | Acceptance criterion | Test | |
| :-- | :--- | :--- | :-- |
| 3.1 | A POST without `MCP-Protocol-Version` is rejected `400` | `test_post_without_protocol_header_is_rejected` | ✅ |
| 3.2 | A header disagreeing with the body returns `-32020` | `test_header_body_mismatch_is_rejected` | ✅ |
| 3.3 | Missing `clientCapabilities` returns `-32602` | `test_missing_client_capabilities_is_invalid_params` | ✅ |
| 3.4 | An unsupported revision returns `-32022` listing supported versions | `test_unsupported_protocol_version_is_rejected` | ✅ |

### MCP-4 — Results

| # | Acceptance criterion | Test | |
| :-- | :--- | :--- | :-- |
| 4.1 | Every result carries `resultType: "complete"` | `test_results_declare_result_type` | ✅ |
| 4.2 | `tools/list` ordering is stable across calls and across fresh servers | `test_tools_list_ordering_is_deterministic` | ✅ |
| 4.3 | All five cacheable methods carry `ttlMs` and `cacheScope` | `test_cacheable_results_carry_ttl_and_scope` | ✅ |

> **On ordering.** The specification asks only for a *deterministic* order, not an alphabetical
> one. Order here is registry order — `(priority, name)` across modules, then declaration order
> within a module — which is reproducible across restarts and is what the test asserts.

### MCP-5 — Single revision ⛔ by decision D-3

The server implements `2026-07-28` only. SDK v2 would otherwise also answer the legacy
`2025-11-25` handshake from the same endpoint with no switch to disable it, so
`ProtocolGuardMiddleware` enforces the policy.

| # | Acceptance criterion | Test | |
| :-- | :--- | :--- | :-- |
| 5.1 | A legacy `initialize` handshake is refused | `test_legacy_initialize_handshake_is_refused` | ✅ |
| 5.2 | `STRICT_PROTOCOL=false` restores dual-revision support | — | 🔍 configuration switch |

### MCP-6 — Out of scope ⛔

| Feature | Reason |
| :--- | :--- |
| `subscriptions/listen` | The tool, resource and prompt surface is static for the process lifetime; no list-changed notification is ever emitted. Revisit if modules become hot-loadable. |
| Roots, Sampling, Logging | Deprecated in 2026-07-28 (SEP-2577). Logging goes to stderr; the agent talks to its LLM provider directly. |
| Tasks extension | No operation exceeds a few seconds. |
| MRTR (`InputRequiredResult`) | No tool requires elicitation. The client still surfaces `input_required` as a clear error rather than crashing. |

---

## 4. Capability surface

### TOOL-1 — Eleven tools

| Tool | Module | Signature |
| :--- | :--- | :--- |
| `get_company` | listings | `symbol: str` |
| `search_companies` | listings | `sector: str \| None`, `market: str \| None` |
| `list_sectors` | listings | — |
| `list_market_resources` | listings | — |
| `read_market_resource` | listings | `uri: str` |
| `get_filings` | filings | `symbol: str`, `filing_type: str \| None` |
| `get_latest_filing` | filings | `symbol: str`, `filing_type: str \| None` |
| `calc_financial_ratios` | analytics | `symbol: str`, `fiscal_period: str \| None` |
| `calc_revenue_growth` | analytics | `symbol: str` |
| `compare_companies` | analytics | `symbols: list[str]` |
| `sector_ranking` | analytics | `sector: str`, `metric: str = "market_cap"`, `top_n: int = 5` |

| # | Acceptance criterion | Test | |
| :-- | :--- | :--- | :-- |
| 1.1 | All eleven appear in `tools/list` | `test_tools_list_contents` | ✅ |
| 1.2 | Each returns a usable payload | `test_every_tool_answers` | ✅ |
| 1.3 | The pinned symbol set keeps its shape | `test_get_company_shape` | ✅ |
| 1.4 | Schemas are valid JSON Schema derived from typed signatures; the injected `Context` never leaks into them | `test_tool_schemas_are_json_schema` | ✅ |
| 1.5 | Tool output is byte-identical to the equivalent REST endpoint | `test_tool_matches_rest_endpoint` | ✅ |
| 1.6 | Missing data returns `{"error": …}`, never an unhandled exception | `test_missing_data_returns_error_payload` | ✅ |
| 1.6a | A missing ticker is a normal result, not a protocol error | `test_missing_symbol_is_a_normal_result` | ✅ |
| 1.7 | An unknown tool reports `is_error` rather than failing the transport | `test_unknown_tool_reports_is_error` | ✅ |
| 1.8 | No dynamic code evaluation anywhere in the backend (AST-checked) | `test_no_exec_anywhere_in_the_backend` | ✅ |
| 1.9 | Nothing writes to stdout outside CLI entrypoints | `test_nothing_writes_to_stdout` | ✅ |

> **On `list[dict] | dict`.** The SDK derives an output schema from the return annotation and
> validates against it. A tool that can return `{"error": …}` must say so in its annotation, or the
> error path fails schema validation and surfaces as `isError`.

### RES-1 — Market resources

Two static resources and three templates. Payloads are identical to the equivalent tool, so the two
surfaces cannot drift.

| URI | Kind |
| :--- | :--- |
| `market://companies` · `market://sectors` | static |
| `market://companies/{symbol}` | template |
| `market://filings/{symbol}/latest` · `market://filings/{symbol}/{period}` | template |

| # | Acceptance criterion | Test | |
| :-- | :--- | :--- | :-- |
| 1.1 | Both static resources are listed | `test_static_resources` | ✅ |
| 1.2 | All three templates are listed | `test_resource_templates` | ✅ |
| 1.3 | A resource payload equals the equivalent tool result | `test_resource_matches_tool_payload` | ✅ |
| 1.4 | A two-parameter template resolves a fiscal period | `test_filing_period_resource` | ✅ |
| 1.5 | Unknown symbol → `-32602` (moved from `-32002` in this revision) | `test_unknown_resource_is_invalid_params` | ✅ |
| 1.6 | Unknown period → `-32602` | `test_unknown_filing_period_is_invalid_params` | ✅ |
| 1.7 | Path traversal is rejected | `test_resource_rejects_path_traversal` | ✅ |

### PROMPT-1 — Domain prompts

| # | Acceptance criterion | Test | |
| :-- | :--- | :--- | :-- |
| 1.1 | `analyze-equity` and `compare-stocks` are declared with their arguments | `test_prompts_are_declared` | ✅ |
| 1.2 | Arguments are rendered into the message text | `test_prompt_renders_its_argument` | ✅ |
| 1.3 | A missing required argument is an error | `test_prompt_missing_argument_is_an_error` | ✅ |

> **Documented deviation.** The specification calls for `-32602` on a missing prompt argument. The
> SDK validates arguments before the handler runs and reports `-32603`. That is SDK behaviour, not a
> choice this server makes, so the test asserts what actually happens and records why.

---

## 5. MCP client

### CLI-1 — Lock-free concurrency

| # | Acceptance criterion | Test | |
| :-- | :--- | :--- | :-- |
| 1.1 | No lock serialises tool execution; the only lock guards connection setup | `test_no_lock_serialises_tool_calls` | ✅ |
| 1.2 | Ten parallel calls finish in far less than ten times one call | `test_concurrent_calls_are_not_serialised` | ✅ |
| 1.3 | The client works end to end against the live backend | `test_client_talks_to_the_live_backend` | ✅ |
| 1.4 | An unreachable gateway produces an actionable message, not a socket error | `test_client_reports_a_useful_error_when_the_gateway_is_down` | ✅ |

### CLI-2 — Correlation

| # | Acceptance criterion | Test | |
| :-- | :--- | :--- | :-- |
| 2.1 | A conversation id travels in the W3C `baggage` key | `test_request_meta_carries_a_conversation_id` | ✅ |
| 2.2 | That id reaches the audit log and is queryable | `test_conversation_id_is_recorded_for_our_own_client` | ✅ |

### AGENT-1 — Streaming and progress

| # | Acceptance criterion | Test | |
| :-- | :--- | :--- | :-- |
| 1.1 | Progress notifications reach the caller on the request's own stream | `test_progress_notifications_reach_the_caller` | ✅ |
| 1.2 | A run emits the AG-UI contract and exactly one usage event | `test_a_run_emits_the_agui_contract` | ✅ `-m llm` |
| 1.3 | The model answers from tool results, not memory | `test_the_model_answers_from_tools_not_memory` | ✅ `-m llm` |
| 1.4 | Reasoning content is never forwarded to the UI | `test_reasoning_is_never_forwarded` | ✅ `-m llm` |

---

## 6. Governance

### GOV-1 — The gateway path

Assertions that cannot be made in-process. Run with `-m gateway` against a live agentgateway.

| # | Acceptance criterion | Test | |
| :-- | :--- | :--- | :-- |
| 1.1 | The revision survives the proxy hop | `test_protocol_survives_the_gateway_hop` | ✅ `-m gateway` |
| 1.2 | All eleven tools are proxied | `test_tools_are_proxied` | ✅ `-m gateway` |
| 1.3 | A tool call returns real data through the gateway | `test_tool_call_returns_real_data_through_the_gateway` | ✅ `-m gateway` |
| 1.4 | The resource bridge works through the gateway | `test_resource_bridge_works_through_the_gateway` | ✅ `-m gateway` |
| 1.5 | A caller's identity reaches the audit log | `test_calls_through_the_gateway_are_attributed` | ✅ `-m gateway` |

### GOV-2 — Measured gateway limitations

Pinned as tests so that a future gateway which fixes them **fails the suite** and tells us to retire
the workaround, rather than the limitation quietly persisting in prose.

| # | Acceptance criterion | Test | |
| :-- | :--- | :--- | :-- |
| 2.1 | The gateway returns empty resource, template and prompt lists | `test_gateway_does_not_proxy_resources_or_prompts` | ✅ `-m gateway` |
| 2.2 | The gateway rewrites `ttlMs` to `0` and `cacheScope` to `private` | `test_gateway_rewrites_cache_hints` | ✅ `-m gateway` |

### GOV-3 — Editable tool allowlist

The allowlist is the governance boundary. It is edited in the gateway's own `config.yaml` so it stays
versioned in git and there is no second source of truth.

| # | Acceptance criterion | Test | |
| :-- | :--- | :--- | :-- |
| 3.1 | The shipped allowlist is readable | `test_reads_the_shipped_allowlist` | ✅ |
| 3.2 | A write round-trips | `test_write_then_read_round_trips` | ✅ |
| 3.3 | Every comment and unrelated key in the config survives a write | `test_write_preserves_the_rest_of_the_config` | ✅ |
| 3.4 | Duplicates collapse | `test_duplicates_are_collapsed` | ✅ |
| 3.5 | An unknown tool is rejected and the file is left byte-identical | `test_unknown_tool_is_rejected` | ✅ |
| 3.6 | Malformed names cannot inject YAML; the file is left byte-identical | `test_malformed_names_cannot_reach_the_file` | ✅ |
| 3.7 | An empty allowlist denies rather than leaving `rules:` dangling | `test_empty_allowlist_denies_rather_than_dangling` | ✅ |
| 3.8 | `ALLOW_POLICY_EDIT=false` refuses the write | `test_editing_can_be_disabled` | ✅ |
| 3.9 | `GET /observability/policy` lists every tool with its permission | `test_policy_endpoint_lists_every_tool` | ✅ |
| 3.10 | `PUT` rejects an unknown tool with `400` | `test_put_policy_rejects_an_unknown_tool` | ✅ |
| 3.11 | `PUT` round-trips and reports `restart_required` | `test_put_policy_round_trips_and_asks_for_a_restart` | ✅ |
| 3.12 | A blocked tool is actually refused by the gateway after restart | — | 🔍 verified manually: `MCPError: Unknown tool: sector_ranking` |

---

## 7. Observability

### OBS-1 — Per-call audit record

| # | Acceptance criterion | Test | |
| :-- | :--- | :--- | :-- |
| 1.1 | Every call writes one row with full detail | `test_record_writes_one_row_with_full_detail` | ✅ |
| 1.2 | Arguments are truncated; payloads are never stored whole | `test_arguments_are_truncated` | ✅ |
| 1.3 | Resource and prompt calls are classified, not just `tools/call` | `test_resource_and_prompt_calls_are_classified` | ✅ |
| 1.4 | Real HTTP traffic is recorded end to end | `test_calls_api_records_real_traffic` | ✅ |

### OBS-2 — Caller attribution

Under 2026-07-28 `clientInfo` travels in `_meta` on every request. That is what makes per-call
attribution possible; earlier revisions announced identity once, at `initialize`.

| # | Acceptance criterion | Test | |
| :-- | :--- | :--- | :-- |
| 2.1 | Known clients resolve to a stable source label | `test_known_clients_resolve_to_a_source` | ✅ |
| 2.2 | An unrecognised client is `unknown` — never guessed | `test_unknown_client_is_never_guessed` | ✅ |
| 2.3 | A gateway route is the fallback when `clientInfo` is unusable | `test_route_hint_is_the_fallback` | ✅ |
| 2.4 | `clientInfo` is extracted correctly, and absence is handled | `test_client_info_extraction` | ✅ |
| 2.5 | A caller with no usable identity is recorded as unknown | `test_unknown_caller_is_recorded_as_unknown` | ✅ |
| 2.6 | Observed `clientInfo` for the four external hosts is recorded in `CLAUDE.md` §6.1 | — | ⛔ open: requires the hosts installed |

### OBS-3 — Conversation and episode

MCP defines no conversation identifier in any revision. Only a client this project controls can
supply one; everything else is grouped by an explicitly-labelled heuristic.

| # | Acceptance criterion | Test | |
| :-- | :--- | :--- | :-- |
| 3.1 | A conversation id is read from the W3C `baggage` key | `test_conversation_id_from_baggage` | ✅ |
| 3.2 | A vendor `_meta` key is picked up with no code change | `test_conversation_id_from_a_vendor_meta_key` | ✅ |
| 3.3 | Unrecognised `_meta` keys are preserved and size-capped | `test_unrecognised_meta_is_preserved_and_capped` | ✅ |
| 3.3a | A W3C `traceparent` yields the trace id for correlation | `test_trace_id_from_traceparent` | ✅ |
| 3.4 | Calls group into episodes by idle gap | `test_episodes_group_by_idle_gap` | ✅ |
| 3.5 | Episodes are per source | `test_episodes_are_per_source` | ✅ |
| 3.6 | The UI labels these `episode`, never chat, thread or session | — | 🔍 see [DESIGN.md](DESIGN.md) |

### OBS-4 — Query API

| # | Acceptance criterion | Test | |
| :-- | :--- | :--- | :-- |
| 4.1 | `/calls` filters by source, tool, status and conversation | `test_query_filters` | ✅ |
| 4.2 | Results are newest-first and paginate | `test_query_is_newest_first_and_paginates` | ✅ |
| 4.3 | `/summary` totals agree with the underlying rows | `test_summary_matches_the_rows` | ✅ |
| 4.4 | `/servers` reports the surface and the allowlist state | `test_summary_and_servers_endpoints` | ✅ |
| 4.5 | Recent callers are listed | `test_callers_seen` | ✅ |
| 4.6 | `/gateway-logs/raw` remains available as the escape hatch | `test_gateway_logs_raw_is_still_available` | ✅ |
| 4.7 | Retention prunes by age | `test_retention_prunes_by_age` | ✅ |
| 4.8 | `/stream` pushes new calls over SSE | — | 🔍 exercised by the Fleet Activity panel |

### OBS-5 — Gateway log ingestion

The in-process recorder sees everything that *reaches* the server. A call the gateway **refused**
never arrives, which is the one thing the ingester adds.

| # | Acceptance criterion | Test | |
| :-- | :--- | :--- | :-- |
| 5.1 | A gateway log line parses into an audit record | `test_ingester_parses_a_gateway_line` | ✅ |
| 5.2 | Unparseable lines are skipped, not guessed at | `test_ingester_skips_unparseable_lines` | ✅ |
| 5.3 | Only denials are ingested, so nothing is double-counted | `test_ingester_records_only_denials` | ✅ |
| 5.4 | A missing log file is handled | `test_ingester_handles_a_missing_file` | ✅ |
| 5.5 | Field mapping matches a real gateway log | — | ⛔ open: written against the documented schema, not captured output |

---

## 8. Frontend

Design authority is [DESIGN.md](DESIGN.md). There is **no automated frontend suite** — this is the
largest open gap in the project, and a CSS regression that broke chat scrolling reached the user
because of it.

| # | Acceptance criterion | | |
| :-- | :--- | :-- | :-- |
| UI-1 | Token counts come only from the backend usage event; the cached-token cell is hidden when unreported | 🔍 | |
| UI-2 | No agent, session id, or context limit is shown that the backend does not report | 🔍 | |
| UI-3 | Declared and gateway-reachable capability counts are shown side by side, never conflated | 🔍 | |
| UI-4 | Conversation scope and fleet scope are visually distinct | 🔍 | |
| UI-5 | `source=unknown` renders as unknown | 🔍 | |
| UI-6 | Every navigation entry resolves to a real view | 🔍 | |
| UI-7 | Every scroll container scrolls; the composer is always reachable | 🔍 | measured at 1440×900, 1200×560 and 390×780 |
| UI-8 | Conversations persist across reload | 🔍 | |

---

## 9. Platform

| # | Acceptance criterion | | |
| :-- | :--- | :-- | :-- |
| PLAT-1 | Python floor is 3.11 (`BaseExceptionGroup`) | ✅ declared in `pyproject.toml` |
| PLAT-2 | No `.exe`, `.bat` or `.ps1` is required to run the stack | ✅ `scripts/dev.py`, `scripts/get_gateway.py` |
| PLAT-3 | No absolute or OS-specific path is hardcoded | ✅ code review + `config.py` |
| PLAT-4 | A fresh clone installs and runs | 🔍 verified end to end on macOS |
| PLAT-5 | `install.bat` / `run.bat` complete on Windows | ⛔ open: syntax-checked only, never executed on Windows |
| PLAT-6 | CI on Linux and Windows | ⛔ open: not yet configured |
