# Copy to frontend/.env.local (optional — the default below is what dev.py uses).
#
# One backend process serves the AG-UI stream, the REST API and the audit API,
# so the UI only needs this single origin.
NEXT_PUBLIC_BACKEND_URL=http://127.0.0.1:8000
