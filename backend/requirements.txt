# --- Web / API ---
fastapi>=0.115
uvicorn[standard]>=0.30

# --- Data layer ---
SQLAlchemy>=2.0
# MCP SDK v2 requires pydantic >= 2.12.
pydantic>=2.12
pydantic-settings>=2.3
Faker>=25.0

# --- MCP (server + client), protocol revision 2026-07-28 ---
# v2 is a breaking rewrite of v1: MCPServer replaces FastMCP, the unified
# Client replaces ClientSession + streamablehttp_client, attributes are
# snake_case, and httpx2 replaces httpx. Do not relax this floor.
mcp>=2.0,<3

# --- AG-UI protocol (agent <-> web chat) ---
ag-ui-protocol>=0.1.7

# --- LLM (OpenAI-compatible client; targets a LiteLLM proxy) ---
openai>=1.40
