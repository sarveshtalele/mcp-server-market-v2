# --- Web / API ---
fastapi>=0.115
uvicorn[standard]>=0.30

# --- Data layer ---
SQLAlchemy>=2.0
pydantic>=2.7
pydantic-settings>=2.3
Faker>=25.0

# --- HTTP client (MCP server -> Data API) ---
httpx>=0.27

# --- MCP (server + client) ---
mcp>=1.2.0

# --- AG-UI protocol (agent <-> CopilotKit) ---
ag-ui-protocol>=0.1.7

# --- LLM (OpenAI-compatible client; targets a LiteLLM proxy) ---
openai>=1.40

# --- Tests (dev) ---
pytest>=8.0
