"""Pluggable domain modules.

Each subpackage defines a top-level ``MODULE = ModuleSpec(...)`` and is
auto-discovered by ``core.registry.discover_modules``. See README.md here for
how to add your own.
"""
